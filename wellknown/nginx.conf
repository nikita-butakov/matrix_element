server {
    listen 8080;
    server_name localhost;

    location /.well-known/matrix/server {
        default_type application/json;
        return 200 '{"m.server": "jitsidevops.website:8448"}';
    }
}
